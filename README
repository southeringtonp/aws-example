To install:

Clone the repo:
    git clone https://github.com/southeringtonp/aws-example

Generate a new SSH key:
    cd aws-example
    ssh-keygen -t rsa -f id_terraform -C "Terraform Key"

Configure AWS Access variables:
    export AWS_ACCESS_KEY_ID="AXXXXXXXXXXXXXXXXXXA"
    export AWS_SECRET_ACCESS_KEY="AxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxA"
    export AWS_DEFAULT_REGION="us-east-1"

Deploy via terraform:
    terraform init
    terraform plan
    terraform apply

